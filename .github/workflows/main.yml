on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:


jobs:
  frequent-task:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run the Task
        run: echo "This action runs every 15 minutes!"
